apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: epik-run-pod-kill
  namespace: epik
spec:
  action: pod-kill
  mode: one
  duration: 1s
  selector:
    labelSelectors:
      app: epik-run
