# Archived (v0)

The legacy CPU-analogy chaos schedule lived here. Infrastructure pod-kill tests
now live in `deploy/chaos/pod-kill.yaml` and `tests/resilience/chaos-pod-kill.yaml`.
Those experiments are biologically inert: they test operator retry, not methylation.
